<UserControl x:Class="Assistance_Prog.MVVM.View.MaidView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Assistance_Prog.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock
                Text="Choose room number"
                TextAlignment="Center"
                Background="White"
                Foreground="Wheat"
                Width="120"
                Style="{StaticResource ComboBoxTheme}"/>
            <ComboBox Name="Room" Width="100"
                Height="20"
                Margin="0,80,0,0"
                ItemsSource="{Binding Rooms, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                SelectedItem="{Binding SelectedRoom}"/>
        </StackPanel>
    </Grid>
</UserControl>
